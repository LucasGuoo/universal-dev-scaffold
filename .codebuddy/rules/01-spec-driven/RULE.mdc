---
description: Spec-Driven 开发流程约定（通用软件工程脚手架）
alwaysApply: true
enabled: true
---

# Spec-Driven 开发流程

本项目采用 Spec-Driven Development（轻量技能化）。进入项目做任何改动前：

1. 先读 `AGENTS.md`，再读 `docs/constitution.md`（不可违背的硬约束）。
2. 按 `AGENTS.md` 的「变更决策树」判断流程：
   - 纯文档 / 单文件 <50 行且无架构变化 → 直接改 + commit 说明
   - 配置/依赖变更、小功能、规则调整 → 写 `specs/active/YYYY-MM-DD-<slug>/change-proposal.md`
   - 多文件 / 新功能 / 协议变更 / 架构变化 → 完整 spec 流程
3. 写 `proposal.md` 前用 **brainstorming** 澄清真实意图（模板顶部「需求澄清」）；写 `tasks.md` 用 **writing-plans** 结构（Files / Interfaces / 2–5 分钟步骤 / 反占位符 / Self-Review）；验收用 `verdict.md` **双检**。
4. 完整 spec 每步必须等用户确认后推进（5 步确认制）。
5. 提交用 Conventional Commits：`type(scope): description`，并说明对应 spec / change-proposal。
6. 换 Agent / 换工具时，在 `tasks.md` 末尾写「接手上下文」后 `git push`，新 Agent `git pull` 接手。
