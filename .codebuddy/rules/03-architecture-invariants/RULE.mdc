---
description: 架构不可变约束（按项目填写）
alwaysApply: true
enabled: true
---

# 架构不可变约束

下列为通用占位。请按本项目在 `docs/constitution.md` 第 10 条之后补充**业务专属不可变约束**（如触发权分离、入站验签、数据不可变、协议兼容等）。

任何改动不得破坏这些约束；若必须破坏，先修订 `constitution.md` 并显式说明。
